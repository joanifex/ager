const start = [
  'grass', 'grass', 'grass',
  'mountain', 'lake', 'grass',
  'forest', 'forest', 'forest',
];

const tiles = (state = start, action) => {
  switch (action.type) {
    default: 
      return state;
  }
}

export default tiles;
